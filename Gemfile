source "https://rubygems.org"

# Match GitHub Pages build environment as closely as possible.
# This pulls in a compatible Jekyll + plugins set.
gem "github-pages", group: :jekyll_plugins

# Required for `jekyll serve` on Ruby 3+ (WEBrick is no longer bundled).
gem "webrick", "~> 1.8"
